const initialState = {
  title: null,
  description: null,
  destinationCount: null,
  destinations: [],
  notes: null
};

const modifyTrip = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
}

module.exports = modifyTrip;
